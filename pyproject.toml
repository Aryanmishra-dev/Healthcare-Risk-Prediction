[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--cov=backend.app",
    "--cov=ml",
    "--cov=shared",
    "--cov-report=term-missing",
    "--cov-report=html:htmlcov",
    "--cov-fail-under=70",
]

[tool.coverage.run]
branch = true
omit = [
    "*/tests/*",
    "*/ml/experiments/*",
    "ml/models/generate_stubs.py",
    "ml/pipelines/evaluation/*",
    "ml/pipelines/training/train.py",
    "ml/pipelines/training/calibrate_lung_model.py",
    "ml/pipelines/training/optuna_optimize.py",
    "ml/pipelines/training/train_heart_disease_model.py",
    "ml/pipelines/training/train_lung_cancer_model.py",
    "ml/pipelines/inference/risk_assistant.py",
    "ml/registry/*",
    "backend/app/api/legacy_main.py",
]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__",
    "raise NotImplementedError",
]
