apiVersion: v1
kind: Secret
metadata:
  name: {{ include "healthpredict.fullname" . }}-secrets
type: Opaque
stringData:
  # WARNING: Do not hardcode secrets in this file.
  # Set these via `helm install --set db.url=...` or an external secrets operator.
  DATABASE_URL: {{ .Values.db.url | quote }}
